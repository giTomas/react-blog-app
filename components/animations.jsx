import React from 'react';
import style from '../styles/style';

const {innerContainer, questrial, title} = style;

const Animations = () => (
  <div>
    <h2 style={{...questrial, ...title}}>Animations</h2>
  </div>
);

export default Animations;

import React from 'react';
import style from '../styles/style';

const {innerContainer, questrial, title} = style;

const Events = () => (
  <div>
    <h2 style={{...questrial, ...title}}>Events</h2>
  </div>
);

export default Events;
